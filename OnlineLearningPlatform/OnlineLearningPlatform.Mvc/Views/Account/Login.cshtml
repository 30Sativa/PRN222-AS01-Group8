
﻿@using OnlineLearningPlatform.Mvc.Models.Account
@model LoginViewModel

@model OnlineLearningPlatform.Mvc.Models.LoginViewModel

@{
    ViewData["Title"] = "Login";
    Layout = null;
}
<link rel="stylesheet" href="~/css/Account/login.css" />
<div class="login-container">
    <div class="login-card">
        <h2>Login</h2>
        <form asp-action="Login" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <input asp-for="Email" class="form-control" placeholder="Email" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input asp-for="Password" type="password" class="form-control" placeholder="Password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary w-100">Login</button>
        </form>

        <div class="login-divider" style="text-align: center; margin-bottom: 15px; color: black">
             <span>hoặc</span>
        </div>


        <form asp-action="LoginWithGoogle" method="post">
            <button type="submit">Login with Google</button>
        </form>

        <div class="mt-3">
            <p>
                Chưa có tài khoản?
                <a asp-controller="Account" asp-action="Register">
                    Đăng ký ngay
                </a>
            </p>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}